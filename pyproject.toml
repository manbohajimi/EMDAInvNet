[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "emdainvnet-reproduction"
version = "0.1.0"
description = "Clean-room reproduction of EMDAInvNet for 3-D GPR permittivity inversion"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "PyYAML>=6.0", "scipy>=1.10", "torch>=2.1", "matplotlib>=3.7"]

[tool.setuptools.packages.find]
where = ["src"]
